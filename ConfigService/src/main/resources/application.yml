---
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
      
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/jferrater/ConfigData


server:
  port: 8167
  
